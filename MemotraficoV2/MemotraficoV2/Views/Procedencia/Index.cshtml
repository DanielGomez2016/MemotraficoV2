@model IEnumerable<MemotraficoV2.Models.Procedencia>
@{
    ViewBag.Title = "Procedencia";
}

<div class="" role="main">
    <div class="">
        <div class="page-title">
            <div class="title_left">

            </div>

        </div>
        <div class="clearfix"></div>
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        @* titulo *@
                        <h2>Procedencia</h2>   @Html.ActionLink("Nueva Procedencia", "Create", null, new { @class = "col-md-offset-med btn btn-primary btn-sm" })
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <table id="datatable" class="table table-striped table-bordered dt-responsive nowrap">
                            <thead>
                                <tr>
                                    <th>Procedencia</th>
                                    <th>Domicilio</th>
                                    <th>Localidad</th>
                                    <th>Contacto</th>
                                    <th>Tipo Procedencia</th>
                                    <th></th>
                                </tr>
                            </thead>

                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Procedencia1</td>
                                        <td>@item.Domicilio</td>
                                        <td>@item.NombreLocalidad, @item.NombreMunicipio</td>
                                        <td>@item.Contacto</td>
                                        <td>@item.TipoProcedencia.TipoProcedencia1</td>
                                        <td>
                                            <div class="btn-group">
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdProcedencia }, new { @class = "btn btn-primary btn-sm" })
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
