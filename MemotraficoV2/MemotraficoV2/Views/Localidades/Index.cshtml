@model IEnumerable<MemotraficoV2.Models.Localidades>

@{
    ViewBag.Title = "Localidades";
}

<div class="" role="main">
    <div class="">
        <div class="page-title">
            <div class="title_left">

            </div>

        </div>
        <div class="clearfix"></div>
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        @* titulo *@
                        <h2>Localidades</h2>   @Html.ActionLink("Nueva Localidad", "Create", null, new { @class = "col-md-offset-med btn btn-primary btn-sm" })
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <table id="datatable" class="table table-striped table-bordered dt-responsive nowrap">
                            <thead>
                                <tr>
                                    <th>Nombre Localidad</th>
                                    <th>Municipio</th>
                                    <th></th>
                                </tr>
                            </thead>

                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Nombre</td>
                                        <td>@item.Municipios.Nombre</td>

                                        <td>
                                            <div class="btn-group">
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdLocalidad }, new { @class = "btn btn-primary btn-sm" })
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
